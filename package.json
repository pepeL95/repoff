{
  "name": "copilot-bridge-monorepo",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "extension"
  ],
  "scripts": {
    "build": "npm run build -w extension",
    "clean": "npm run clean -w extension",
    "package:extension": "npm run package -w extension"
  }
}
